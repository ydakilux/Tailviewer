<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dashboard="clr-namespace:Tailviewer.Ui.SidePanel">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../../Themes/Constants.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="SidePanelToggleButtonStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource SecondaryLightBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="{DynamicResource SecondaryLighterBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type dashboard:SidePanelControl}">
        <Setter Property="Background" Value="{StaticResource SecondaryBrush}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type dashboard:SidePanelControl}">
                    <Border Background="{TemplateBinding Background}">
                        <Grid>
                            <ItemsControl ItemsSource="{TemplateBinding SidePanels}"
                                          FocusVisualStyle="{x:Null}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"
                                                    HorizontalAlignment="Right" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type dashboard:ISidePanelViewModel}">
                                        <Border BorderThickness="1,0,0,0"
                                                BorderBrush="{StaticResource PrimarySeparatorBrush}">
                                            <ToggleButton IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                          ToolTip="{Binding Tooltip}"
                                                          Style="{StaticResource SidePanelToggleButtonStyle}"
                                                          FocusVisualStyle="{x:Null}">
                                                <Grid>
                                                    <Viewbox Margin="12">
                                                        <Canvas Width="24" Height="24">
                                                            <Path
                                                                Data="{Binding Icon}"
                                                                Fill="{StaticResource PrimaryForegroundBrush}" />
                                                        </Canvas>
                                                    </Viewbox>
                                                    <TextBlock Text="{Binding QuickInfo}"
                                                               FontSize="10"
                                                               TextWrapping="NoWrap"
                                                               TextTrimming="CharacterEllipsis"
                                                               VerticalAlignment="Bottom"
                                                               Foreground="{StaticResource PrimaryForegroundBrush}" />
                                                </Grid>
                                            </ToggleButton>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
