<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                    xmlns:controls="clr-namespace:Tailviewer.Ui.QuickFilter">

    <Style TargetType="{x:Type controls:FilterToggleButton}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid>
                        <Viewbox Width="16" Height="16" Stretch="Uniform">
                            <Canvas Width="24" Height="24">
                                <iconPacks:PackIconMaterial x:Name="PART_Icon"
                                      Width="24" Height="24"
                                      Foreground="#444444" />
                            </Canvas>
                        </Viewbox>
                    </Grid>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType={x:Type controls:FilterToggleButton}}}"
                                     Value="False">
                            <Setter TargetName="PART_Icon"
                                    Property="Kind"
                                    Value="Equal" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType={x:Type controls:FilterToggleButton}}}"
                                     Value="True">
                            <Setter TargetName="PART_Icon"
                                    Property="Kind"
                                    Value="NotEqual" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>